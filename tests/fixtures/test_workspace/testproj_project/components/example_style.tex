The algorithm proceeds as follows. First, we initialize the parameters.
Then, we iterate until convergence is achieved. Finally, we return the result.

Note that this approach has been validated extensively in prior work.
